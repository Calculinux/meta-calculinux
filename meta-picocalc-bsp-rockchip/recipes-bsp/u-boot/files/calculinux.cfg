# Calculinux PicoCalc / Luckfox Lyra overrides on top of Armbian's
# luckfox-lyra-rk3506_defconfig (armbian/build#10470).

# Prefer the base Lyra DT (RK3506G), not Lyra Plus
CONFIG_DEFAULT_DEVICE_TREE="rk3506-luckfox-lyra"
CONFIG_DEFAULT_FDT_FILE="rockchip/rk3506-luckfox-lyra.dtb"

# RAUC / FIT boot.scr needs these (disabled in Armbian defconfig)
CONFIG_CMD_SETEXPR=y
CONFIG_CMD_SETEXPR_FMT=y
CONFIG_CMD_PART=y
CONFIG_CMD_SOURCE=y
CONFIG_PARTITION_UUIDS=y
CONFIG_EFI_PARTITION=y

# bootm a standard mkimage FIT (no Rockchip post-process / HW crypto)
CONFIG_FIT=y
CONFIG_CMD_BOOTM=y
CONFIG_CMD_ITEST=y

# load /data/fit/zboot_merged_*.img from OVERLAY_DATA
CONFIG_FS_EXT4=y
CONFIG_CMD_FS_GENERIC=y

# Env in the GPT ubootenv partition (starts at 12 MiB on the WIC image).
# Kept below ROOT_A at 16 MiB so OTA can rewrite U-Boot without moving rootfs.
CONFIG_ENV_IS_IN_MMC=y
CONFIG_ENV_REDUNDANT=y
CONFIG_ENV_SIZE=0x8000
CONFIG_ENV_OFFSET=0xC00000
CONFIG_ENV_OFFSET_REDUND=0xC08000

CONFIG_BOOTDELAY=5

# Host tool needs gnutls PKCS#11 we don't ship; not used on this board
# CONFIG_TOOLS_MKEFICAPSULE is not set

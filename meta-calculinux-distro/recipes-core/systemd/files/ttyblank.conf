[Service]
# Enable UTF-8 mode on the console for proper character rendering
# unicode_start is provided by the kbd package
ExecStartPre=/bin/sh -c 'unicode_start 2>/dev/null || true'
# Set screen blanking timeout
ExecStartPost=/bin/sh -c 'setterm --blank 5 >/dev/tty1'
